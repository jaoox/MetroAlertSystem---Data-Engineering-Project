package simulation.movement

import simulation.model._

object PersonMovement {
  def updatePersonPosition(people: List[Person]): Unit = {
    // Mettre à jour la position de chaque personne en fonction de sa vitesse de déplacement
    ???
  }
}